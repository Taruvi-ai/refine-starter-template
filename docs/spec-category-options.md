# Kaizen Category Options

Add `Revenue Generation` and `TAT` to the shared Kaizen category list and the live `kaizen_dropdown_options` configuration. The options must be available anywhere the shared category dropdown is used, without changing existing records or removing the current Productivity, Quality, and Other choices.
